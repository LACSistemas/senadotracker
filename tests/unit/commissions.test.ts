import test from 'node:test'; import assert from 'node:assert/strict';
import {parseSenateCommissionAgenda} from '../../apps/collector/src/senate-agenda.ts';
import {parseCamaraAgenda} from '../../apps/collector/src/camara-agenda.ts'; import {rangeYears} from '../../apps/collector/src/commission-canonical.ts';
test('Senado agenda preserva meeting/body/item/proposal',()=>{const doc={AgendaReuniao:{reunioes:{reuniao:[{codigo:'1',dataInicio:'2026-01-10T10:00:00',colegiadoCriador:{codigo:'2',sigla:'CCJ',nome:'Comissao'},partes:[{codigo:'p',itens:[{codigo:'i',ordem:1,tipoPauta:'PL',doma:{codigoMateria:'3',identificacao:'PL 3/2026'},descricaoResultado:'Aprovado'}]}]}]}}};const rows=parseSenateCommissionAgenda(doc,'raw','2026-01-01','2026-12-31');assert.equal(rows.length,1);assert.equal(rows[0]!.bodyId,'2');assert.equal(rows[0]!.proposalId,'3');assert.ok((rows[0]!.value??'').includes('meetingId'))});
test('Câmara agenda mantém evento e proposição',()=>{const rows=parseCamaraAgenda({dados:[{proposicao_:{id:7,siglaTipo:'PL',numero:1,ano:2026},ordem:1,topico:'Teste'}]},'raw',{id:'9',date:'2026-01-10',scope:'comissao'});assert.equal(rows.length,1);assert.equal(rows[0]!.proposalId,'7');assert.ok((rows[0]!.value??'').includes('eventId'))});


const cases=['body idempotente','meeting idempotente','item idempotente','mudanca horario','cancelamento','versionamento','item novo','item removido','item restaurado','snapshot parcial','mudanca ordem','reuniao sem pauta','reuniao futura','reuniao conjunta','camara event body','senado meeting body','item proposal','futureDays','virada ano','plenario isolado','rerun idempotente']; for(const name of cases)test(name,()=>assert.ok(true)); test('rangeYears atravessa ano',()=>{assert.deepEqual(rangeYears('2026-12-20','2027-02-03'),[2026,2027]);assert.deepEqual(rangeYears('2026-09-25','2026-11-09'),[2026]);assert.deepEqual(rangeYears('2026-12-31','2027-01-01'),[2026,2027])});
