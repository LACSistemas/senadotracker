import test from 'node:test';import assert from 'node:assert/strict';import {parseSenateCabinetPayroll} from '../../apps/collector/src/cabinet.ts';
const header=['VÍNCULO','CATEGORIA','CARGO','REFERÊNCIA CARGO','SÍMBOLO FUNÇÃO','ANO EXERCÍCIO','LOTAÇÃO EXERCÍCIO','TIPO FOLHA','REMUN_BASICA','VANT_PESSOAIS','FUNC_COMISSIONADA','GRAT_NATALINA','HORAS_EXTRAS','OUTRAS_EVENTUAIS','ABONO_PERMANENCIA','REVERSAO_TETO_CONST','IMPOSTO_RENDA','PREVIDÊNCIA','FALTAS','REM_LIQUIDA','DIÁRIAS','AUXÍLIOS','VANT_INDENIZATORIAS'];
test('folha agrega gabinete por competência e mantém rubricas separadas',()=>{const row=['COMISSIONADO','','AP-01','','','2025','Gabinete do Senador Alan Rick','Normal','1000,00','100,00','50,00','0','0','0','0','-10,00','-100','-100','-5,00','935,00','20,00','30,00','40,00'];const csv=`ÚLTIMA ATUALIZAÇÃO;31/08/2026 05:01\n${header.join(';')}\n${row.join(';')}\n`;const bytes=Buffer.from(csv,'latin1');const parsed=parseSenateCabinetPayroll(bytes,'raw','2026-08',new Map([['alan rick','5672']]));assert.equal(parsed.matchedOffices,1);assert.equal(parsed.costs.find(x=>x.rubric==='cabinet_payroll_gross')!.valueCents,113500);assert.equal(parsed.costs.find(x=>x.rubric==='cabinet_allowances')!.valueCents,3000);assert.equal(parsed.costs.find(x=>x.rubric==='cabinet_staff_normal_rows')!.count,1)});
