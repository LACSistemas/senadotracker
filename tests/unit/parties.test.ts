import test from 'node:test';import assert from 'node:assert/strict';import type { LegislativeVote,Profile } from '@senadotracker/domain';import { groupRecordedPartyVotes,partyAtDate } from '@senadotracker/db';
const profile={source:'senado',externalId:'1',name:'Pessoa',fullName:null,uf:'SP',party:'ATUAL',photoUrl:null,officialUrl:'https://example.invalid',observedAt:'2026-09-09T12:00:00Z',historyAvailability:'available',mandates:[],exercises:[],events:[],rawId:'raw',parties:[{key:'a',partyId:null,party:'ANTIGO',mandateKey:null,start:'2025-01-01',end:'2025-12-31',endReason:'reported',precision:'date',basis:'official',rawId:'raw'},{key:'b',partyId:null,party:'ATUAL',mandateKey:null,start:'2026-01-01',end:null,endReason:'not_reported',precision:'date',basis:'official',rawId:'raw'}]} satisfies Profile;
test('filiação partidária é resolvida na data da observação',()=>{assert.equal(partyAtDate(profile,'2025-06-01'),'ANTIGO');assert.equal(partyAtDate(profile,'2026-06-01'),'ATUAL');assert.equal(partyAtDate(profile,'2024-06-01'),null)});
test('comportamento partidário agrega apenas votos registrados',()=>{const vote=(party:string|null,value:string,id:string):LegislativeVote=>({source:'senado',deliberationId:'d',externalId:id,vote:value,description:null,party,uf:'SP',recordedAt:null,rawId:'raw'});const groups=groupRecordedPartyVotes([vote('AAA','Sim','1'),vote('AAA','Não','2'),vote('AAA','Sim','3'),vote(null,'Não participou','4')]);assert.deepEqual(groups,[{party:'AAA',total:3,votes:[{label:'Sim',value:2},{label:'Não',value:1}]}]);assert.equal(JSON.stringify(groups).includes('participou'),false)});
